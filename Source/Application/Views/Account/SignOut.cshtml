@model SignOutViewModel
<div class="card">
	<div class="card-header">
		<h1 class="h4 m-0 p-0">Sign out</h1>
	</div>
	<div class="card-body">
		Do you want to sign out?
		<partial model="@Model.Form" name="SignOutForm" />
	</div>
</div>